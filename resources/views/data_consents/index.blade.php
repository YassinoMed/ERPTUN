@extends('layouts.admin')
@section('page-title'){{ __('Data Consents') }}@endsection
@section('breadcrumb')<li class="breadcrumb-item"><a href="{{ route('dashboard') }}">{{ __('Dashboard') }}</a></li><li class="breadcrumb-item">{{ __('Data Consents') }}</li>@endsection
@section('action-btn')<div class="float-end d-flex">@can('create data consent')<a href="#" data-url="{{ route('data-consents.create') }}" data-size="lg" data-ajax-popup="true" data-title="{{ __('Create Data Consent') }}" class="btn btn-sm btn-primary"><i class="ti ti-plus"></i></a>@endcan</div>@endsection
@section('content')
<div class="card"><div class="card-body table-border-style"><div class="table-responsive"><table class="table datatable"><thead><tr><th>{{ __('Subject') }}</th><th>{{ __('Type') }}</th><th>{{ __('Purpose') }}</th><th>{{ __('Consent Date') }}</th><th>{{ __('Status') }}</th><th width="220px">{{ __('Action') }}</th></tr></thead><tbody>@foreach($consents as $consent)<tr><td><div>{{ $consent->subject_name }}</div><small class="text-muted">{{ $consent->subject_reference ?: '-' }}</small></td><td>{{ $consent->subject_type ?: '-' }}</td><td>{{ $consent->purpose ?: '-' }}</td><td>{{ $consent->consented_at ? Auth::user()->dateFormat($consent->consented_at) : '-' }}</td><td>{{ ucfirst($consent->status) }}</td><td class="Action"><div class="action-btn me-2"><a href="{{ route('data-consents.show', $consent->id) }}" class="mx-3 btn btn-sm align-items-center bg-warning"><i class="ti ti-eye text-white"></i></a></div>@can('edit data consent')<div class="action-btn me-2"><a href="#" data-url="{{ route('data-consents.edit', $consent->id) }}" data-size="lg" data-ajax-popup="true" data-title="{{ __('Edit Data Consent') }}" class="mx-3 btn btn-sm align-items-center bg-info"><i class="ti ti-pencil text-white"></i></a></div>@endcan @can('delete data consent')<div class="action-btn">{!! Form::open(['method' => 'DELETE', 'route' => ['data-consents.destroy', $consent->id], 'id' => 'delete-form-consent-' . $consent->id]) !!}<a href="#" class="mx-3 btn btn-sm align-items-center bs-pass-para bg-danger" data-confirm="{{ __('Are You Sure?') . '|' . __('This action can not be undone. Do you want to continue?') }}" data-confirm-yes="document.getElementById('delete-form-consent-{{ $consent->id }}').submit();"><i class="ti ti-trash text-white"></i></a>{!! Form::close() !!}</div>@endcan</td></tr>@endforeach</tbody></table></div></div></div>
@endsection
