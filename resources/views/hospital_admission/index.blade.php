@extends('layouts.admin')
@section('page-title'){{ __('Hospital Admissions') }}@endsection
@section('breadcrumb')<li class="breadcrumb-item"><a href="{{ route('dashboard') }}">{{ __('Dashboard') }}</a></li><li class="breadcrumb-item">{{ __('Hospital Admissions') }}</li>@endsection
@section('action-btn')<div class="float-end d-flex">@can('create hospital admission')<a href="#" data-url="{{ route('hospital-admissions.create') }}" data-size="xl" data-ajax-popup="true" data-title="{{ __('Create Hospital Admission') }}" class="btn btn-sm btn-primary"><i class="ti ti-plus"></i></a>@endcan</div>@endsection
@section('content')
<div class="card"><div class="card-body table-border-style"><div class="table-responsive"><table class="table datatable"><thead><tr><th>{{ __('Patient') }}</th><th>{{ __('Doctor') }}</th><th>{{ __('Admission') }}</th><th>{{ __('Room/Bed') }}</th><th>{{ __('Status') }}</th><th>{{ __('Action') }}</th></tr></thead><tbody>@foreach($admissions as $admission)<tr><td><a href="{{ route('hospital-admissions.show', $admission->id) }}" class="text-primary">{{ optional($admission->patient)->first_name }} {{ optional($admission->patient)->last_name }}</a></td><td>{{ optional($admission->doctor)->name ?? '-' }}</td><td>{{ \Auth::user()->dateFormat($admission->admission_date) }} {{ \Auth::user()->timeFormat($admission->admission_date) }}</td><td>{{ optional($admission->room)->name ?? '-' }} / {{ optional($admission->bed)->bed_number ?? '-' }}</td><td>{{ ucfirst($admission->status) }}</td><td>@can('show hospital admission')<a href="{{ route('hospital-admissions.show', $admission->id) }}" class="btn btn-sm bg-warning"><i class="ti ti-eye text-white"></i></a>@endcan @can('edit hospital admission')<a href="#" class="btn btn-sm bg-info" data-url="{{ route('hospital-admissions.edit', $admission->id) }}" data-ajax-popup="true" data-size="xl" data-title="{{ __('Edit Admission') }}"><i class="ti ti-pencil text-white"></i></a>@endcan @can('delete hospital admission'){!! Form::open(['method'=>'DELETE','route'=>['hospital-admissions.destroy',$admission->id],'id'=>'delete-hadm-'.$admission->id,'class'=>'d-inline']) !!}<a href="#" class="btn btn-sm bg-danger bs-pass-para" data-confirm="{{ __('Are You Sure?').'|'.__('This action can not be undone. Do you want to continue?') }}" data-confirm-yes="document.getElementById('delete-hadm-{{ $admission->id }}').submit();"><i class="ti ti-trash text-white"></i></a>{!! Form::close() !!}@endcan</td></tr>@endforeach</tbody></table></div></div></div>
@endsection
