@extends('layouts.admin')
@section('page-title'){{ __('GDPR Register') }}@endsection
@section('breadcrumb')<li class="breadcrumb-item"><a href="{{ route('dashboard') }}">{{ __('Dashboard') }}</a></li><li class="breadcrumb-item">{{ __('GDPR Register') }}</li>@endsection
@section('action-btn')<div class="float-end d-flex">@can('create gdpr activity')<a href="#" data-url="{{ route('gdpr-activities.create') }}" data-size="lg" data-ajax-popup="true" data-title="{{ __('Create GDPR Activity') }}" class="btn btn-sm btn-primary"><i class="ti ti-plus"></i></a>@endcan</div>@endsection
@section('content')
<div class="card"><div class="card-body table-border-style"><div class="table-responsive"><table class="table datatable"><thead><tr><th>{{ __('Activity') }}</th><th>{{ __('Purpose') }}</th><th>{{ __('Lawful Basis') }}</th><th>{{ __('Retention') }}</th><th>{{ __('Status') }}</th><th width="220px">{{ __('Action') }}</th></tr></thead><tbody>@foreach($activities as $activity)<tr><td><div>{{ $activity->activity_name }}</div><small class="text-muted">{{ $activity->activity_code }}</small></td><td>{{ $activity->purpose ?: '-' }}</td><td>{{ $activity->lawful_basis ?: '-' }}</td><td>{{ $activity->retention_period ?: '-' }}</td><td>{{ ucfirst(str_replace('_', ' ', $activity->status)) }}</td><td class="Action"><div class="action-btn me-2"><a href="{{ route('gdpr-activities.show', $activity->id) }}" class="mx-3 btn btn-sm align-items-center bg-warning"><i class="ti ti-eye text-white"></i></a></div>@can('edit gdpr activity')<div class="action-btn me-2"><a href="#" data-url="{{ route('gdpr-activities.edit', $activity->id) }}" data-size="lg" data-ajax-popup="true" data-title="{{ __('Edit GDPR Activity') }}" class="mx-3 btn btn-sm align-items-center bg-info"><i class="ti ti-pencil text-white"></i></a></div>@endcan @can('delete gdpr activity')<div class="action-btn">{!! Form::open(['method' => 'DELETE', 'route' => ['gdpr-activities.destroy', $activity->id], 'id' => 'delete-form-gdpr-' . $activity->id]) !!}<a href="#" class="mx-3 btn btn-sm align-items-center bs-pass-para bg-danger" data-confirm="{{ __('Are You Sure?') . '|' . __('This action can not be undone. Do you want to continue?') }}" data-confirm-yes="document.getElementById('delete-form-gdpr-{{ $activity->id }}').submit();"><i class="ti ti-trash text-white"></i></a>{!! Form::close() !!}</div>@endcan</td></tr>@endforeach</tbody></table></div></div></div>
@endsection
