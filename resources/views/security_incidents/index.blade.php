@extends('layouts.admin')
@section('page-title'){{ __('Security Incidents') }}@endsection
@section('breadcrumb')<li class="breadcrumb-item"><a href="{{ route('dashboard') }}">{{ __('Dashboard') }}</a></li><li class="breadcrumb-item">{{ __('Security Incidents') }}</li>@endsection
@section('action-btn')<div class="float-end d-flex">@can('create security incident')<a href="#" data-url="{{ route('security-incidents.create') }}" data-size="lg" data-ajax-popup="true" data-title="{{ __('Create Security Incident') }}" class="btn btn-sm btn-primary"><i class="ti ti-plus"></i></a>@endcan</div>@endsection
@section('content')
<div class="card"><div class="card-body table-border-style"><div class="table-responsive"><table class="table datatable"><thead><tr><th>{{ __('Incident') }}</th><th>{{ __('Type') }}</th><th>{{ __('Severity') }}</th><th>{{ __('Owner') }}</th><th>{{ __('Detected') }}</th><th>{{ __('Status') }}</th><th width="220px">{{ __('Action') }}</th></tr></thead><tbody>@foreach($incidents as $incident)<tr><td><div>{{ $incident->title }}</div><small class="text-muted">{{ $incident->incident_reference }}</small></td><td>{{ $incident->incident_type ?: '-' }}</td><td>{{ ucfirst($incident->severity) }}</td><td>{{ optional($incident->owner)->name ?: '-' }}</td><td>{{ $incident->detected_at ? Auth::user()->dateFormat($incident->detected_at) : '-' }}</td><td>{{ ucfirst($incident->status) }}</td><td class="Action"><div class="action-btn me-2"><a href="{{ route('security-incidents.show', $incident->id) }}" class="mx-3 btn btn-sm align-items-center bg-warning"><i class="ti ti-eye text-white"></i></a></div>@can('edit security incident')<div class="action-btn me-2"><a href="#" data-url="{{ route('security-incidents.edit', $incident->id) }}" data-size="lg" data-ajax-popup="true" data-title="{{ __('Edit Security Incident') }}" class="mx-3 btn btn-sm align-items-center bg-info"><i class="ti ti-pencil text-white"></i></a></div>@endcan @can('delete security incident')<div class="action-btn">{!! Form::open(['method' => 'DELETE', 'route' => ['security-incidents.destroy', $incident->id], 'id' => 'delete-form-sec-' . $incident->id]) !!}<a href="#" class="mx-3 btn btn-sm align-items-center bs-pass-para bg-danger" data-confirm="{{ __('Are You Sure?') . '|' . __('This action can not be undone. Do you want to continue?') }}" data-confirm-yes="document.getElementById('delete-form-sec-{{ $incident->id }}').submit();"><i class="ti ti-trash text-white"></i></a>{!! Form::close() !!}</div>@endcan</td></tr>@endforeach</tbody></table></div></div></div>
@endsection
