@extends('layouts.admin')
@section('page-title', __('Advanced Preferences'))
@section('page-subtitle', __('Tune workspace density, notifications and shortcuts for a more focused daily workflow.'))
@section('content')<div class="card ux-list-card"><div class="card-body"><form method="POST" action="{{ route('core.preferences.update') }}">@csrf <div class="row"><div class="col-md-4 mb-3"><label class="form-label">{{ __('Theme Mode') }}</label><select name="theme_mode" class="form-control"><option value="light" {{ \Auth::user()->theme_mode === 'light' ? 'selected' : '' }}>Light</option><option value="dark" {{ \Auth::user()->theme_mode === 'dark' ? 'selected' : '' }}>Dark</option></select><small class="text-muted">{{ __('Controls default workspace contrast.') }}</small></div><div class="col-md-4 mb-3"><label class="form-label">{{ __('Notification Mode') }}</label><select name="pwa_notifications" class="form-control"><option value="enabled" {{ \Auth::user()->pwa_notifications === 'enabled' ? 'selected' : '' }}>Enabled</option><option value="digest" {{ \Auth::user()->pwa_notifications === 'digest' ? 'selected' : '' }}>Digest</option><option value="disabled" {{ \Auth::user()->pwa_notifications === 'disabled' ? 'selected' : '' }}>Disabled</option></select><small class="text-muted">{{ __('Choose between instant alerts or digest mode.') }}</small></div><div class="col-md-4 mb-3"><label class="form-label">{{ __('Keyboard Shortcuts') }}</label><input type="text" name="keyboard_shortcuts" class="form-control" value="{{ \Auth::user()->keyboard_shortcuts }}" placeholder="{{ __('Ctrl+K, N, / ...') }}"><small class="text-muted">{{ __('Personal reminder field for the commands you actually use.') }}</small></div></div><button class="btn btn-primary">{{ __('Save Preferences') }}</button></form></div></div>@endsection
