@extends('layouts.admin')
@section('page-title'){{ __('Pharmacy Dispensing') }}@endsection
@section('breadcrumb')<li class="breadcrumb-item"><a href="{{ route('dashboard') }}">{{ __('Dashboard') }}</a></li><li class="breadcrumb-item">{{ __('Pharmacy Dispensing') }}</li>@endsection
@section('action-btn')<div class="float-end d-flex">@can('create pharmacy dispensation')<a href="#" data-url="{{ route('pharmacy-dispensations.create') }}" data-size="xl" data-ajax-popup="true" data-title="{{ __('Create Dispensation') }}" class="btn btn-sm btn-primary"><i class="ti ti-plus"></i></a>@endcan</div>@endsection
@section('content')
<div class="card"><div class="card-body table-border-style"><div class="table-responsive"><table class="table datatable"><thead><tr><th>{{ __('Patient') }}</th><th>{{ __('Dispensed At') }}</th><th>{{ __('Status') }}</th><th>{{ __('Action') }}</th></tr></thead><tbody>@foreach($dispensations as $dispensation)<tr><td><a href="{{ route('pharmacy-dispensations.show', $dispensation->id) }}" class="text-primary">{{ optional($dispensation->patient)->first_name }} {{ optional($dispensation->patient)->last_name }}</a></td><td>{{ \Auth::user()->dateFormat($dispensation->dispensed_at) }} {{ \Auth::user()->timeFormat($dispensation->dispensed_at) }}</td><td>{{ ucfirst($dispensation->status) }}</td><td>@can('show pharmacy dispensation')<a href="{{ route('pharmacy-dispensations.show', $dispensation->id) }}" class="btn btn-sm bg-warning"><i class="ti ti-eye text-white"></i></a>@endcan @can('edit pharmacy dispensation')<a href="#" class="btn btn-sm bg-info" data-url="{{ route('pharmacy-dispensations.edit', $dispensation->id) }}" data-ajax-popup="true" data-size="xl" data-title="{{ __('Edit Dispensation') }}"><i class="ti ti-pencil text-white"></i></a>@endcan @can('delete pharmacy dispensation'){!! Form::open(['method'=>'DELETE','route'=>['pharmacy-dispensations.destroy',$dispensation->id],'id'=>'delete-pdisp-'.$dispensation->id,'class'=>'d-inline']) !!}<a href="#" class="btn btn-sm bg-danger bs-pass-para" data-confirm="{{ __('Are You Sure?').'|'.__('This action can not be undone. Do you want to continue?') }}" data-confirm-yes="document.getElementById('delete-pdisp-{{ $dispensation->id }}').submit();"><i class="ti ti-trash text-white"></i></a>{!! Form::close() !!}@endcan</td></tr>@endforeach</tbody></table></div></div></div>
@endsection
