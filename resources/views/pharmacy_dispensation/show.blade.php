@extends('layouts.admin')
@section('page-title'){{ __('Pharmacy Dispensation') }}@endsection
@section('breadcrumb')<li class="breadcrumb-item"><a href="{{ route('dashboard') }}">{{ __('Dashboard') }}</a></li><li class="breadcrumb-item"><a href="{{ route('pharmacy-dispensations.index') }}">{{ __('Pharmacy Dispensing') }}</a></li><li class="breadcrumb-item">{{ __('Details') }}</li>@endsection
@section('content')
<div class="row"><div class="col-lg-4"><div class="card"><div class="card-body"><h5>{{ __('Dispensation Summary') }}</h5><div>{{ __('Patient') }}: {{ optional($dispensation->patient)->first_name }} {{ optional($dispensation->patient)->last_name }}</div><div>{{ __('Date') }}: {{ \Auth::user()->dateFormat($dispensation->dispensed_at) }} {{ \Auth::user()->timeFormat($dispensation->dispensed_at) }}</div><div>{{ __('Status') }}: {{ ucfirst($dispensation->status) }}</div><div>{{ __('Dispensed By') }}: {{ optional($dispensation->dispenser)->name ?? '-' }}</div><div>{{ __('Notes') }}: {{ $dispensation->notes ?? '-' }}</div></div></div></div><div class="col-lg-8"><div class="card"><div class="card-body table-border-style"><div class="table-responsive"><table class="table"><thead><tr><th>{{ __('Medication') }}</th><th>{{ __('Qty') }}</th><th>{{ __('Dosage') }}</th><th>{{ __('Frequency') }}</th><th>{{ __('Duration') }}</th></tr></thead><tbody>@foreach($dispensation->items as $item)<tr><td>{{ optional($item->medication)->name }}</td><td>{{ $item->quantity }}</td><td>{{ $item->dosage ?? '-' }}</td><td>{{ $item->frequency ?? '-' }}</td><td>{{ $item->duration ?? '-' }}</td></tr>@endforeach</tbody></table></div></div></div></div></div>
@endsection
