@extends('layouts.admin')
@section('page-title'){{ __('Pharmacy Stock') }}@endsection
@section('breadcrumb')<li class="breadcrumb-item"><a href="{{ route('dashboard') }}">{{ __('Dashboard') }}</a></li><li class="breadcrumb-item">{{ __('Pharmacy Stock') }}</li>@endsection
@section('action-btn')<div class="float-end d-flex">@can('create pharmacy medication')<a href="#" data-url="{{ route('pharmacy-medications.create') }}" data-ajax-popup="true" data-title="{{ __('Create Medication') }}" class="btn btn-sm btn-primary"><i class="ti ti-plus"></i></a>@endcan</div>@endsection
@section('content')
<div class="card"><div class="card-body table-border-style"><div class="table-responsive"><table class="table datatable"><thead><tr><th>{{ __('Name') }}</th><th>{{ __('Lot') }}</th><th>{{ __('Expiry') }}</th><th>{{ __('Stock') }}</th><th>{{ __('Reorder') }}</th><th>{{ __('Action') }}</th></tr></thead><tbody>@foreach($medications as $medication)<tr><td>{{ $medication->name }}</td><td>{{ $medication->lot_number ?? '-' }}</td><td>{{ $medication->expiry_date ? \Auth::user()->dateFormat($medication->expiry_date) : '-' }}</td><td>{{ $medication->stock_quantity }}</td><td>{{ $medication->reorder_level }}</td><td>@can('edit pharmacy medication')<a href="#" class="btn btn-sm bg-info" data-url="{{ route('pharmacy-medications.edit', $medication->id) }}" data-ajax-popup="true" data-title="{{ __('Edit Medication') }}"><i class="ti ti-pencil text-white"></i></a>@endcan @can('delete pharmacy medication'){!! Form::open(['method'=>'DELETE','route'=>['pharmacy-medications.destroy',$medication->id],'id'=>'delete-pmed-'.$medication->id,'class'=>'d-inline']) !!}<a href="#" class="btn btn-sm bg-danger bs-pass-para" data-confirm="{{ __('Are You Sure?').'|'.__('This action can not be undone. Do you want to continue?') }}" data-confirm-yes="document.getElementById('delete-pmed-{{ $medication->id }}').submit();"><i class="ti ti-trash text-white"></i></a>{!! Form::close() !!}@endcan</td></tr>@endforeach</tbody></table></div></div></div>
@endsection
