@extends('layouts.admin')
@section('page-title'){{ __('Software Licenses') }}@endsection
@section('breadcrumb')<li class="breadcrumb-item"><a href="{{ route('dashboard') }}">{{ __('Dashboard') }}</a></li><li class="breadcrumb-item">{{ __('Software Licenses') }}</li>@endsection
@section('action-btn')<div class="float-end d-flex">@can('create software license')<a href="#" data-url="{{ route('software-licenses.create') }}" data-size="lg" data-ajax-popup="true" data-title="{{ __('Create Software License') }}" class="btn btn-sm btn-primary"><i class="ti ti-plus"></i></a>@endcan</div>@endsection
@section('content')
<div class="card"><div class="card-body table-border-style"><div class="table-responsive"><table class="table datatable"><thead><tr><th>{{ __('License') }}</th><th>{{ __('Vendor') }}</th><th>{{ __('Assigned') }}</th><th>{{ __('Seats') }}</th><th>{{ __('Renewal') }}</th><th>{{ __('Status') }}</th><th width="220px">{{ __('Action') }}</th></tr></thead><tbody>@foreach($licenses as $license)<tr><td><div>{{ $license->name }}</div><small class="text-muted">{{ $license->license_type ?: '-' }}</small></td><td>{{ $license->vendor_name ?: '-' }}</td><td>{{ optional($license->assignedUser)->name ?: '-' }}</td><td>{{ $license->seats_used }}/{{ $license->seats_total }}</td><td>{{ $license->renewal_date ? Auth::user()->dateFormat($license->renewal_date) : '-' }}</td><td>{{ ucfirst($license->status) }}</td><td class="Action"><div class="action-btn me-2"><a href="{{ route('software-licenses.show', $license->id) }}" class="mx-3 btn btn-sm align-items-center bg-warning"><i class="ti ti-eye text-white"></i></a></div>@can('edit software license')<div class="action-btn me-2"><a href="#" data-url="{{ route('software-licenses.edit', $license->id) }}" data-size="lg" data-ajax-popup="true" data-title="{{ __('Edit Software License') }}" class="mx-3 btn btn-sm align-items-center bg-info"><i class="ti ti-pencil text-white"></i></a></div>@endcan @can('delete software license')<div class="action-btn">{!! Form::open(['method' => 'DELETE', 'route' => ['software-licenses.destroy', $license->id], 'id' => 'delete-form-license-' . $license->id]) !!}<a href="#" class="mx-3 btn btn-sm align-items-center bs-pass-para bg-danger" data-confirm="{{ __('Are You Sure?') . '|' . __('This action can not be undone. Do you want to continue?') }}" data-confirm-yes="document.getElementById('delete-form-license-{{ $license->id }}').submit();"><i class="ti ti-trash text-white"></i></a>{!! Form::close() !!}</div>@endcan</td></tr>@endforeach</tbody></table></div></div></div>
@endsection
